function parseLine(line){const out=[];let cur='',quoted=false;for(let i=0;i<line.length;i++){const c=line[i];if(c==='"'){if(quoted&&line[i+1]==='"'){cur+='"';i++}else quoted=!quoted}else if(c===','&&!quoted){out.push(cur);cur=''}else cur+=c}out.push(cur);return out}
export function parseCSV(text=''){const lines=String(text).replace(/^\uFEFF/,'').split(/\r?\n/).filter(x=>x.trim().length);if(!lines.length)return[];const headers=parseLine(lines[0]).map(x=>x.trim());return lines.slice(1).map(line=>{const values=parseLine(line),row={};headers.forEach((h,i)=>row[h]=values[i]??'');return row})}
export function canonicalCSVTemplate(){return 'season,week,playerId,name,position,nflTeam,opponent,projection,actual,cv,status,source,projectionTimestamp\n2025,1,example-1,Example Player,WR,NE,NYJ,14.5,17.2,0.42,ACTIVE,provider,2025-09-04T12:00:00Z\n'}
