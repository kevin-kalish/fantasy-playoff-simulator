import {calibrationSnapshot,bucketedCVForProjection} from './empirical-calibration.js';
import {recommendedCorrelationPriors} from './empirical-correlation.js';
import {simulatePlayer} from '../simulator.js';
import {createSeededRng} from '../random.js';
import {summarizeBacktest,calibrationPenalty} from '../backtest.js';
const DEFAULT_CV={QB:.27,RB:.40,WR:.47,TE:.48,K:.45,DEF:.50};
const q=(xs,p)=>{if(!xs.length)return null;const a=[...xs].sort((x,y)=>x-y),x=(a.length-1)*p,l=Math.floor(x),h=Math.ceil(x);return a[l]+(a[h]-a[l])*(x-l)};
function before(a,b){return a.season<b.season||(a.season===b.season&&a.week<b.week)}
function forecast(row,cv,bias,rng,draws){const projection=Math.max(0,Number(row.projection)+(bias?.additive||0)),sims=Array.from({length:draws},()=>simulatePlayer(projection,row.position,rng,cv));return{projection:Number(row.projection),adjustedProjection:projection,actual:Number(row.actual??row.actualPoints),position:row.position,p10:q(sims,.1),p25:q(sims,.25),p50:q(sims,.5),p75:q(sims,.75),p90:q(sims,.9)};}
function score(f){return{...f,absoluteError:Math.abs(f.adjustedProjection-f.actual),squaredError:(f.adjustedProjection-f.actual)**2,covered50:f.actual>=f.p25&&f.actual<=f.p75,covered80:f.actual>=f.p10&&f.actual<=f.p90,belowP10:f.actual<f.p10,aboveP90:f.actual>f.p90};}
export function runRollingCalibration(rows,{draws=1500,seed=20260923,minTrainingRows=100,minPositionRows=30,buckets=4,shrinkage=40}={}){const ordered=[...rows].filter(r=>Number.isFinite(Number(r.projection))&&Number.isFinite(Number(r.actual??r.actualPoints))).sort((a,b)=>a.season-b.season||a.week-b.week),rngFixed=createSeededRng(seed),rngEmp=createSeededRng(seed+1),rngBucket=createSeededRng(seed+2),fixed=[],empirical=[],bucketed=[],folds=[];const periods=[...new Set(ordered.map(r=>`${r.season}:${r.week}`))];for(const period of periods){const [season,week]=period.split(':').map(Number),train=ordered.filter(r=>before(r,{season,week})),test=ordered.filter(r=>r.season===season&&r.week===week);if(train.length<minTrainingRows||!test.length)continue;const snap=calibrationSnapshot(train,{minRows:minPositionRows,buckets,shrinkage}),corr=recommendedCorrelationPriors(train);for(const r of test){const parent=snap.positionCV[r.position]??DEFAULT_CV[r.position]??.42;fixed.push(score(forecast(r,DEFAULT_CV[r.position]??.42,null,rngFixed,draws)));empirical.push(score(forecast(r,parent,snap.bias[r.position],rngEmp,draws)));bucketed.push(score(forecast(r,bucketedCVForProjection(snap.projectionBuckets,r.position,r.projection,parent),snap.bias[r.position],rngBucket,draws)));}folds.push({season,week,trainingRows:train.length,testRows:test.length,positionCV:snap.positionCV,projectionBuckets:snap.projectionBuckets,bias:snap.bias,correlation:corr});}const fixedSummary=summarizeBacktest(fixed),empiricalSummary=summarizeBacktest(empirical),bucketedSummary=summarizeBacktest(bucketed),allFixed=fixedSummary.find(x=>x.position==='ALL'),allEmp=empiricalSummary.find(x=>x.position==='ALL'),allBucket=bucketedSummary.find(x=>x.position==='ALL');if(!folds.length||!allFixed||!allEmp||!allBucket||!allFixed.n||!allEmp.n||!allBucket.n)return{rows:ordered.length,folds,comparison:null,byPosition:{fixed:fixedSummary,empirical:empiricalSummary,bucketed:bucketedSummary},recommendation:'insufficient-data',adoptEmpirical:false,latestParameters:null};const scored=x=>({...x,calibrationPenalty:calibrationPenalty(x)}),comparison={fixed:scored(allFixed),empirical:scored(allEmp),bucketed:scored(allBucket)};const candidates=Object.entries(comparison).filter(([name,x])=>name==='fixed'||x.rmse<=comparison.fixed.rmse*1.01).sort((a,b)=>a[1].calibrationPenalty-b[1].calibrationPenalty||a[1].rmse-b[1].rmse),recommendation=candidates[0]?.[0]||'fixed';return{rows:ordered.length,folds,comparison,byPosition:{fixed:fixedSummary,empirical:empiricalSummary,bucketed:bucketedSummary},recommendation,adoptEmpirical:recommendation!=='fixed',latestParameters:folds.at(-1)||null};}
export function summarizeRollingCalibration(result){return{rows:result.rows,folds:result.folds.length,recommendation:result.recommendation,comparison:result.comparison,latestParameters:result.latestParameters&&{season:result.latestParameters.season,week:result.latestParameters.week,positionCV:result.latestParameters.positionCV,projectionBuckets:result.latestParameters.projectionBuckets,bias:result.latestParameters.bias,correlation:result.latestParameters.correlation}};}
