const DEFAULT_CV={QB:.27,RB:.40,WR:.47,TE:.48,K:.45,DEF:.50};
const mean=xs=>xs.length?xs.reduce((a,b)=>a+b,0)/xs.length:null;
const quantile=(xs,q)=>{if(!xs.length)return null;const a=[...xs].sort((x,y)=>x-y),p=(a.length-1)*q,l=Math.floor(p),h=Math.ceil(p);return a[l]+(a[h]-a[l])*(p-l)};
const sd=xs=>{if(xs.length<2)return null;const m=mean(xs);return Math.sqrt(xs.reduce((s,x)=>s+(x-m)**2,0)/(xs.length-1))};
const clamp=(x,lo,hi)=>Math.max(lo,Math.min(hi,x));
export function projectionErrorProfile(rows,{minProjection=3,minRows=30}={}){const by={};for(const r of rows){const p=Number(r.projection),a=Number(r.actual??r.actualPoints);if(!Number.isFinite(p)||!Number.isFinite(a)||p<minProjection)continue;(by[r.position]||=[]).push({p,a,error:a-p,ratio:a/Math.max(p,.1)});}const out={};for(const [position,g] of Object.entries(by)){if(g.length<minRows)continue;const errors=g.map(x=>x.error),ratios=g.map(x=>x.ratio),projs=g.map(x=>x.p),actuals=g.map(x=>x.a),residSd=sd(errors),projMean=mean(projs);out[position]={n:g.length,bias:mean(errors),mae:mean(errors.map(Math.abs)),rmse:Math.sqrt(mean(errors.map(x=>x*x))),errorSd:residSd,projectionMean:projMean,actualMean:mean(actuals),cv:residSd/Math.max(projMean,1),ratioMedian:quantile(ratios,.5),ratioP10:quantile(ratios,.1),ratioP90:quantile(ratios,.9)};}return out;}
export function calibratedPositionCV(rows,opts={}){const profiles=projectionErrorProfile(rows,opts),out={...DEFAULT_CV};for(const [pos,p] of Object.entries(profiles))out[pos]=clamp(p.cv,.12,1.10);return out;}
export function projectionBiasAdjustments(rows,opts={}){const profiles=projectionErrorProfile(rows,opts),out={};for(const [pos,p] of Object.entries(profiles))out[pos]={additive:p.bias,multiplier:p.projectionMean>0?p.actualMean/p.projectionMean:1,n:p.n};return out;}
export function projectionBucketProfile(rows,{minProjection=3,minPositionRows=30,buckets=4,shrinkage=40}={}){const positionCV=calibratedPositionCV(rows,{minProjection,minRows:minPositionRows}),by={};for(const r of rows){const p=Number(r.projection),a=Number(r.actual??r.actualPoints);if(!Number.isFinite(p)||!Number.isFinite(a)||p<minProjection)continue;(by[r.position]||=[]).push({p,error:a-p});}const out={};for(const [position,g0] of Object.entries(by)){if(g0.length<minPositionRows)continue;const g=[...g0].sort((a,b)=>a.p-b.p),parent=positionCV[position]??DEFAULT_CV[position]??.42,parts=[];for(let b=0;b<buckets;b++){const lo=Math.floor(g.length*b/buckets),hi=Math.floor(g.length*(b+1)/buckets),part=g.slice(lo,hi);if(!part.length)continue;const projectionMean=mean(part.map(x=>x.p)),errorSd=sd(part.map(x=>x.error)),rawCv=errorSd==null?parent:errorSd/Math.max(projectionMean,1),weight=part.length/(part.length+shrinkage),cv=clamp(weight*rawCv+(1-weight)*parent,.12,1.10);parts.push({minProjection:b===0?0:part[0].p,maxProjection:b===buckets-1?Infinity:part.at(-1).p,n:part.length,projectionMean,errorSd,rawCv,cv,weight});}out[position]={parentCV:parent,buckets:parts};}return out;}
export function bucketedCVForProjection(model,position,projection,fallback=null){const p=Number(projection),entry=model?.[position];if(!entry?.buckets?.length)return fallback??entry?.parentCV??DEFAULT_CV[position]??.42;const bucket=entry.buckets.find((b,i)=>p>=b.minProjection&&(i===entry.buckets.length-1||p<=b.maxProjection));return bucket?.cv??entry.parentCV??fallback??DEFAULT_CV[position]??.42;}
export function calibrationSnapshot(rows,opts={}){const profiles=projectionErrorProfile(rows,opts),positionCV=calibratedPositionCV(rows,opts),bias=projectionBiasAdjustments(rows,opts),projectionBuckets=projectionBucketProfile(rows,{minProjection:opts.minProjection,minPositionRows:opts.minRows,buckets:opts.buckets??4,shrinkage:opts.shrinkage??40});return{profiles,positionCV,bias,projectionBuckets};}
